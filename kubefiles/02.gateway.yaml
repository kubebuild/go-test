apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: go-test-gateway
  namespace: go-test
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - test.cloudhero.io
